{
  "name": "your-hugo-site",
  "version": "1.0.0",
  "scripts": {
    "build": "hugo --gc --minify",
    "vercel-build": "hugo --gc --minify"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18"
  }
}